apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gpu-operator.fullname" . }}-test-connection"
  labels:
{{ include "gpu-operator.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "gpu-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
